# Phase 02 — Contextual recommendation notifications

- Status: active
- Drafted: 2026-08-30
- Started: 2026-08-30
- Branch: `feature/02-contextual-notifications`
- Product authority: `docs/GOAL.md`
- Activation gate: met on 2026-08-30; Phase 01 is accepted and merged into
  `develop`, and the owner directed continued development of the registered
  presence-aware notification scope

## Objective

Deliver useful recommendation summaries only to configured occupants who are
currently home, and give an arriving occupant a fresh, still-relevant summary
instead of replaying changes that happened while everybody was away.

## Frozen decisions

- This phase delivers notifications only; it never controls a window, blind,
  shutter, awning, HVAC system, or other physical actuator.
- The existing stable-change candidate is the sole ordinary delivery trigger.
  Repeated evaluations with unchanged resolved targets do not notify.
- Presence comes from configured `person` entities, their native Mobile App
  `device_tracker` relationships, and native `home` states/transitions, not
  coordinates or deprecated device triggers.
- Each recipient stores only one `person`. The adapter joins that person's
  Mobile App trackers to sibling `notify` entities through Home Assistant's
  entity/device registries and targets every linked tracker currently `home`.
  Registry `device_id` is used only for this native relationship join; the
  fixed `notify.send_message` action receives entity IDs. Configuration stores
  neither targets, arbitrary action strings, nor inferred names.
- Recipients are repeatable config subentries and duplicate persons are
  rejected. Schema-v4 revision 2 removes the beta's redundant stored
  notification target; revision-1 recipient subentries migrate in place by
  retaining their person only. The major schema remains v4 so the verified
  `v0.1.0b5` downgrade stays loadable.
- Away-time transition messages are neither delivered nor queued. Arrival
  causes a fresh evaluation; only current, actionable advice is eligible.
- Missing/degraded recommendation data never becomes favourable advice and is
  not delivered as an actionable recommendation.

## Frozen tasks

Ponytail keeps one person-only subentry, one direct entity/device-registry join,
the existing grouped candidate, and native state transitions. No configured
target, queue, helper, custom recipient registry, service-name parser, new
entity, dependency, or persisted presence ledger is introduced unless a frozen
acceptance test proves the native boundary is insufficient.

| ID | Task and acceptance | Status | Owner | Planned write set | Dependencies / provenance |
|---|---|---|---|---|---|
| P02-T01 | Add a typed recipient contract that maps one configured `person` entity to one explicitly selected native `notify` entity. Accept recipient-subentry creation/reconfigure selection, duplicate and missing-target validation, privacy-safe diagnostics, reload and unchanged-v4 coverage, and a production-boundary test against the registered `notify.send_message` surface. Do not infer names or accept a service string. | terminada | root manager | `const.py`, `config_flow.py`, `application/notifications.py`, `diagnostics.py`, translations, config-flow/init/diagnostics/application tests, `docs/GOAL.md`, ADR and this plan | Native recipient subentries validate entity domains, current/registered targets, duplicate people/targets, and the fixed action surface while preserving schema v4. Diagnostics expose only a count. Focused 22/22 and canonical 155/155 pass at 97.00%; consolidated correctness and Ponytail reviews found no defect or removable layer. |
| P02-T02 | Deliver at most one consolidated message per accepted grouped window/blind transition, and only to configured recipients whose `person` state is `home` at delivery time. Accept present/away/mixed-recipient tests, deterministic room/opening ordering, no delivery for unchanged or degraded results, isolated delivery failure, and zero actuator calls. | terminada | root manager retained after executor permission mismatch | `application/notifications.py`, `adapters/notifications.py`, `coordinator.py`, translations, focused application/adapter/coordinator tests, ADR and this plan | The coordinator consumes only the existing grouped candidate; one translated, deterministically ordered summary reaches each available home recipient through fixed `notify.send_message`. Away/unchanged/unavailable cases send nothing, failures are isolated/redacted, and invalid recipient data cannot degrade climate evaluation. Focused 10/10 and canonical 159/159 pass at 96.66%. |
| P02-T03 | On a configured recipient's real transition into `home`, run a fresh evaluation and send only that arriving recipient a consolidated summary that is still actionable. Accept no stale backlog replay, one delivery per away-to-home transition, restart-safe deduplication, no message when feedback proves targets already satisfied, and explicit wording for manual blinds whose applied position cannot be observed. | terminada | root manager retained after executor permission mismatch | `application/notifications.py`, `adapters/notifications.py`, `coordinator.py`, focused application/notification-adapter/coordinator/init tests, ADR and this plan | Native non-home-to-`home` edges trigger one fresh evaluation and target only the arriving configured person; startup, repeated-home and unavailable recovery do not. Observed satisfied targets are omitted, manual positions remain explicit, and the arriving person is excluded from any simultaneous ordinary candidate. Focused 20/20 and canonical 164/164 pass at 95.85%. |
| P02-T04 | Validate notification behaviour in Home Assistant before release. Accept setup/unload/reload/restart, unavailable person/notify target and send-failure recovery, present/away/mixed/arrival scenarios, Recorder-safe privacy evidence, exact notification counts, no duplicates, no owned persistent backlog, and zero physical services/actions. | en curso | root manager | integration tests, manifest/changelog/version, deployment/status/runbook docs, this plan, Git/HACS release state and Home Assistant external verification | `v0.2.0b4` is installed with the three owner-authorized recipients after a supported full backup and clean restart. Structural, privacy and safety gates pass; the next natural stable change must verify the multiline presentation, and the remaining present/away/mixed/arrival counts still require natural evidence. Retain live-verified `v0.1.0b5` as operational rollback; `v4.17_pre` remains only an immutable fixture. |
| P02-T05 | Replace the redundant person-to-notify selection with native person-to-Mobile-App-device discovery. Accept person-only create/reconfigure, schema-v4 revision-1→2 recipient migration with downgrade compatibility, all-and-only home linked-device delivery, multiple devices per person, duplicate-call suppression, missing/disabled/unavailable registry paths, redacted diagnostics, and a production-boundary test using registry relationships rather than names. Publish/install `v0.2.0b2`, register all four current persons through supported subentry flows, and verify all five associated Mobile App device links while deduplicating any shared notification endpoint, without sending a synthetic message or creating physical actions. | terminada | root manager retained: public schema/migration and prior executor permission mismatch | `AGENTS.md`, `docs/GOAL.md`, `const.py`, `__init__.py`, `config_flow.py`, `application/notifications.py`, `adapters/notifications.py`, translations, focused config/migration/application/delivery/init tests, ADR/README/changelog/version/deployment status/runbook/this plan, Git/HACS release state and supported Home Assistant config flows | Published/installed b2; the native form registered 4/4 persons and resolves five person-device associations to four enabled/available unique targets because one endpoint is shared across persons. Config/reload/restart, schema/structure/identity, diagnostics/privacy, error/action, duplicate suppression, downgrade, and zero-synthetic-message gates pass. |
| P02-T06 | Correct the live recipient scope after the owner clarified that the shared Mobile App phone belongs only to the owner for notification purposes. Accept a read-only impact check proving the owner and `Codex` person share that endpoint, remove only the `Codex` recipient through the supported subentry API, retain the owner's route and every other configured person/device, and verify one loaded entry with 3 recipients, four distinct eligible Mobile App routes, clean reload, diagnostics/log/Repair/privacy gates, and no synthetic message or physical action. Correct living recipient/rollback wording exposed by the audit without changing production code or the underlying Home Assistant person/device associations. | terminada | root manager: protected live config and living references | `docs/GOAL.md`, `docs/status/phase02-notifications.md`, this plan, supported Home Assistant subentry removal and read-only verification | Only the `Codex` subentry was removed. The owner and two other recipients remain; four configured device links now resolve to four distinct enabled/available targets, while the owner's underlying shared tracker relation is untouched. Entry reload, diagnostics, registry inventory, Repairs/log/services/backlog, privacy, and zero-message/action gates pass. |
| P02-T07 | Make ordinary and arrival notifications scannable. Accept separate translated window/blind sections; room-only labels when a room has one opening; one non-duplicated room/opening label when it has several; only components that actually changed or remain actionable; concise weather-safety reasons from the same evaluated policy result; deterministic ordering; preserved manual-blind wording; and no safety-policy, recipient, entity, queue, dependency, service, or actuator change. | terminada | root manager retained: shared application/notification contract and deployment follow-up | `application/state.py`, `application/notifications.py`, `adapters/notifications.py`, focused state/application/delivery tests, ADR/README/GOAL/plan, P02-T04 beta metadata and deployment evidence after local acceptance | Published and installed `v0.2.0b3` through the existing Git/HACS route after exact-output tests, a supported backup and one recovered restart. The component remains loaded with its existing schema, structure and recipient count; natural rendering on a real device remains evidence for P02-T04, not an implementation blocker for the verified formatter. |
| P02-T08 | Complete the notification cutover to the custom component. Accept a read-only provenance and consumer audit, prove whether the legacy advisor automation still executes a notification action, disable only that exact automation through Home Assistant's supported service, retain its immutable repository fixture and a reversible rollback, and verify the automation remains present but off while b3 stays loaded with three recipients and its native notification surface. Do not send a synthetic message, change recipient/config-entry data, or touch any physical action. | terminada | root manager: protected Home Assistant mutation and living migration contract | supported Home Assistant automation/config/trace/consumer reads and `automation.turn_off`, `docs/GOAL.md`, notification status and this plan | Zero automation/script/scene/config-entry callers were found. The dashboard has display-only references and already contains all 17 component entities. The exact legacy automation was turned off reversibly; b3 remains loaded with schema v4, 4 rooms, 5 openings, 3 recipients and the native notification action. No message, recipient/config-entry change or physical action occurred. |
| P02-T09 | Make rain safety façade- and gust-driven. Preserve the absolute all-façade gust close, but when rain has no meaningful gust keep the optimizer's window target on every façade; when rain is wind-driven restrict only exposed façades, while leeward façades retain the optimizer target until the absolute gust limit. Accept cardinal/intercardinal façade matrices, zero/near-zero gust, directional boundary/margin, protected tilt geometry, intense-gust all-close, missing/stale fail-safe, production-boundary direction delivery, notification reasons, replay, and the non-closed/positive-blind invariant. Reuse the configured typed direction source unless a read-only capability audit proves a distinct gust-direction source exists; do not invent a selector or weaken missing/stale safety handling. | terminada | root manager retained: public weather-safety contract, release and protected deployment | `domain/policy.py`, policy/application/replay/notification tests, production adapter boundary, ADR 0007, `docs/GOAL.md`, README/changelog/version, release/runbook/status docs, this plan and supported Git/HACS/Home Assistant deployment | Published and installed b4 applies rain handling only at 0.5 km/h raw projected gust and otherwise continues through normal wind handling. No distinct gust-direction source exists, so schema/configuration remain unchanged. Focused/canonical gates, full backup, exact HACS download, one restart and the redacted post-live gate pass; the legacy automation remains off and the custom component is the sole active advice route. |
| P02-T10 | Restore the owner-confirmed six-room/seven-opening topology—Salón, Baño, Despacho, Cocina, Anto and Eli—and assign each room its display device's one native temperature and humidity sensor. Accept reuse of the exact Salón, Despacho and Cocina identities; deterministic reuse of the generic bedroom identity/opening for Anto and creation of Eli with the same historically frozen north-east geometry; creation of Baño with its historically frozen south-west overhang/protection geometry and the migration's uniform 1.60 m width estimate; supported room/opening create/reconfigure flows only; preservation of optional CO2 and every unaffected global/opening/recipient field; duplicate-link rejection; transactional rollback; one reload; and a redacted post-live gate proving 6 rooms, 7 openings, 8 integration devices, 23 unique entities, all room sources available, all seven recommendations restored when no unrelated input is degraded, legacy automation still off, and zero entity renames, synthetic messages, physical actions, Repairs, integration logs/services or owned persistent notifications. | terminada | root manager: protected live topology/source assignment and supported config-entry mutation | `docs/GOAL.md`, read-only Home Assistant device/entity/config-flow/history audit, predecessor geometry/handoff, supported room/opening subentry create/reconfigure/reload APIs, `docs/status/phase02-notifications.md` and this plan; no production-code, schema, entity-registry or dashboard write | Six exact room-labelled temperature/humidity pairs are configured. The supported flows now expose 6 rooms/7 openings/3 recipients, 8 devices and 23 unique available entities; diagnostics report schema v4, 14 ready source classes and seven valid recommendation/blind/safety triples. The legacy automation remains off; 17 pre-existing dashboard entity references still resolve; Repairs, integration errors/services, owned persistent notifications and invalid targets are zero. Focused 32/32 and canonical 179/179 pass at 95.75%. The first failed opening submission caused extra recovery reloads and left four desired room updates applied because null optional fields also invalidated their rollback; immediate read-back exposed it and the corrected forward completion omitted nulls. No entity registry rename, synthetic message or physical action occurred. |

## Exact local acceptance commands

Each task runs its focused suite first and then the repository canonical command:

| Task | Focused command |
|---|---|
| P02-T01 | `wsl --cd "$PWD" -- bash -lc 'UV_PROJECT_ENVIRONMENT="$HOME/.cache/window-climate-advisor/venv" "$HOME/.local/bin/uv" run --frozen python -m pytest --no-cov tests/unit/application/test_notifications.py tests/integration/test_config_flow.py tests/integration/test_init.py tests/integration/test_diagnostics.py -q'` |
| P02-T02 | `wsl --cd "$PWD" -- bash -lc 'UV_PROJECT_ENVIRONMENT="$HOME/.cache/window-climate-advisor/venv" "$HOME/.local/bin/uv" run --frozen python -m pytest --no-cov tests/unit/application/test_notifications.py tests/integration/test_notification_delivery.py tests/integration/test_coordinator.py -q'` |
| P02-T03 | `wsl --cd "$PWD" -- bash -lc 'UV_PROJECT_ENVIRONMENT="$HOME/.cache/window-climate-advisor/venv" "$HOME/.local/bin/uv" run --frozen python -m pytest --no-cov tests/unit/application/test_notifications.py tests/integration/test_notification_delivery.py tests/integration/test_coordinator.py tests/integration/test_init.py -q'` |
| P02-T04 | `wsl --cd "$PWD" -- bash -lc 'UV_PROJECT_ENVIRONMENT="$HOME/.cache/window-climate-advisor/venv" "$HOME/.local/bin/uv" run --frozen python -m pytest --no-cov tests/integration -q'` |
| P02-T05 | `wsl --cd "$PWD" -- bash -lc 'UV_PROJECT_ENVIRONMENT="$HOME/.cache/window-climate-advisor/venv" "$HOME/.local/bin/uv" run --frozen python -m pytest --no-cov tests/unit/application/test_notifications.py tests/integration/test_config_flow.py tests/integration/test_notification_delivery.py tests/integration/test_init.py -q'` |
| P02-T06 | `uv run --frozen python -c "from scripts.verify import check_artifacts; raise SystemExit(0 if check_artifacts() else 1)"` |
| P02-T07 | `wsl --cd "$PWD" -- bash -lc 'UV_PROJECT_ENVIRONMENT="$HOME/.cache/window-climate-advisor/venv" "$HOME/.local/bin/uv" run --frozen python -m pytest --no-cov tests/unit/application/test_state.py tests/unit/application/test_notifications.py tests/integration/test_notification_delivery.py -q'` |
| P02-T08 | `wsl --cd "$PWD" -- bash -lc 'UV_PROJECT_ENVIRONMENT="$HOME/.cache/window-climate-advisor/venv" "$HOME/.local/bin/uv" run --frozen python -m pytest --no-cov tests/integration/test_notification_delivery.py tests/integration/test_init.py -q'` |
| P02-T09 | `wsl --cd "$PWD" -- bash -lc 'UV_PROJECT_ENVIRONMENT="$HOME/.cache/window-climate-advisor/venv" "$HOME/.local/bin/uv" run --frozen python -m pytest --no-cov tests/unit/domain/test_policy.py tests/unit/application/test_evaluator.py tests/unit/application/test_state.py tests/integration/test_adapters.py tests/integration/test_notification_delivery.py tests/replay -q'` |
| P02-T10 | `wsl --cd "$PWD" -- bash -lc 'UV_PROJECT_ENVIRONMENT="$HOME/.cache/window-climate-advisor/venv" "$HOME/.local/bin/uv" run --frozen python -m pytest --no-cov tests/integration/test_config_flow.py tests/integration/test_adapters.py tests/integration/test_init.py tests/integration/test_diagnostics.py -q'` |

```powershell
uv run --frozen python scripts/verify.py
```

## Stop conditions

- Stop if delivery would require coordinates, an inferred action name, a raw
  unvalidated service string, or a deprecated device trigger.
- Stop if an arrival path would replay historical messages instead of evaluating
  the current recommendation.
- Stop if notification failure can alter optimizer, safety, availability, or
  actuator state.
- Stop for owner review before adding a recipient abstraction, queue, helper,
  entity, dependency, or platform not required by the frozen consumers.

## Phase acceptance

Phase 02 may close only when all P02 tasks are terminated with evidence, the
canonical gate and Ponytail gates pass, a reversible Home Assistant deployment
proves the configured presence/delivery matrix, and no physical action or stale
away-time notification occurs.

## Modification log

| Date | Task | Files/change | Verification |
|---|---|---|---|
| 2026-08-30 | P02-T01–P02-T04 | Reserved the owner-requested next-version presence-aware delivery contract as an inactive draft. Split recipient configuration, ordinary stable-change delivery, fresh arrival advice, and deployed verification into bounded tasks. | Contract review confirms native `person` presence, explicit validated notification actions, no name inference, no away-time queue, no physical action, and no Phase 01 implementation change. |
| 2026-08-30 | P02-T01 | Activated the phase after Phase 01 acceptance/merge and froze the minimum implementation around recipient subentries, native person state, native notify entities, the fixed `notify.send_message` action, the existing grouped candidate, and arrival edges without a queue or presence ledger. Updated the living product contract and exact task write sets/commands. | Read-only local Core 2026.8 source and live capability checks confirm the native notify entity/action boundary with available person and notify targets. Existing schema-v4 entries remain valid with zero recipients; no Home Assistant state changed and no private identifier or raw state was recorded. |
| 2026-08-30 | P02-T01 | Added the pure typed recipient mapping, native repeatable recipient subentry flow, explicit entity/action existence checks, duplicate rejection, unchanged-v4 lifecycle coverage, translations, count-only diagnostics, and ADR 0010. No delivery call, service string, queue, helper, entity, dependency, migration, or physical action was added. | Focused gate: 22 passed. Canonical gate: 155 passed at 97.00% with Ruff, format, strict mypy, artifact/secret, migration, reload, translation, privacy, and diff checks green. Ponytail review: `Lean already. Ship.` |
| 2026-08-30 | P02-T02 | Started the authorized `luna_executor` wave at clean commit `1bc6097`, with an isolated write set and external/docs/Git prohibitions. The executor verified the expected Luna/max identity but received live `danger-full-access` instead of its global `workspace-write` profile and stopped before editing; root retained the task rather than accepting a silent permission mismatch. | No executor artifact or repository change exists. This is a tooling/profile exception, not a product blocker; the root continues the same frozen minimum under its existing session permissions. |
| 2026-08-30 | P02-T02 | Root added the native notification adapter and one coordinator hook. It filters presence and target availability at delivery time, reuses existing entity translations, sorts one grouped room/opening summary, calls only fixed `notify.send_message`, and isolates each `HomeAssistantError`. The consolidated review excluded recipient-only corruption from the climate duplicate-link gate and removed an unnecessary log index. | Focused gate: 10 passed. Canonical gate: 159 passed at 96.66% with Ruff, format, strict mypy, artifact/secret, replay, translation, failure-isolation, and zero-physical-call checks green. The first focused attempt failed before collection because unit and integration modules shared `test_notifications.py`; the living plan and integration filename were corrected, then both gates passed. Final Ponytail review: `Lean already. Ship.` |
| 2026-08-30 | P02-T03 | Started the arrival-context slice after accepting P02-T02. Root retained the isolated implementation because the prior executor launch did not receive its required workspace-only permission profile. | Frozen minimum: only a native non-home-to-`home` edge, one fresh evaluation, only the arriving configured recipient, no startup/restoration delivery, no queue, and no actuator call. |
| 2026-08-30 | P02-T03 | Added transient arrival-edge capture, fresh actionable-advice construction, targeted native delivery, simultaneous ordinary-message exclusion, feedback-based suppression, and explicit manual-blind wording. Kept the implementation lazy outside arrival events and removed redundant validation from its internal transfer value during the Ponytail review. | Focused gate: 20 passed. Canonical gate: 164 passed at 95.85% with Ruff, format, strict mypy, artifact/secret, replay, restart/recovery, privacy, and zero-physical-call checks green. The first event test incorrectly waited on Home Assistant's debouncer and left a timer; it was corrected to isolate the native edge and invoke the fresh evaluation explicitly. Final Ponytail review: `Lean already. Ship.` |
| 2026-08-30 | P02-T04 | Started the release/deployment gate after accepting all local notification slices. Froze `v0.2.0b1`, the reversible fallbacks, and the boundary that recipient mappings must be selected explicitly rather than inferred from private names or devices. | No Home Assistant or Git release state changed at task start. Local setup/reload/restart, delivery-matrix, privacy, failure-isolation, count, no-backlog, and zero-actuator evidence will be consolidated before deployment. |
| 2026-08-30 | P02-T04 | Prepared the numbered notification beta locally: bumped the manifest/test contract, separated release notes, updated the HACS installation/rollback runbook and README, and created the redacted Phase 02 status record. The existing entry schema remains v4 and `v0.1.0b5` remains the integration fallback. | Integration gate: 50 passed. Canonical gate: 164 passed at 95.85% with Ruff, format, strict mypy, artifact/secret, lifecycle, delivery matrix, replay, privacy, and zero-actuator checks green. No Home Assistant, tag, release, or recipient mapping changed during this step. |
| 2026-08-30 | P02-T04 | Published candidate commit `6793d03` as remote `release/0.2.0`, annotated tag and public GitHub prerelease `v0.2.0b1`. After a valid configuration check and a new completed supported backup with database, enabled beta tracking only for this HACS repository, confirmed the exact prerelease, downloaded it through HACS, and performed the required restart. No recipient was inferred or configured. | Post-restart read-only gates confirm Home Assistant 2026.8.3, installed b1/schema v4, one loaded entry, 4 rooms, 5 openings, 6 devices, 17 unique entities, recipient-flow support, zero duplicates/Repairs/integration logs/services/owned persistent notifications, and available baseline. Availability settled from an immediate 7/17 poll to 15/17; diagnostics explicitly attribute the remaining blind/safety pair to one missing input while 11/12 source classes and four recommendations are ready. Recipient count remains zero, so no message was sent. P02-T04 remains in progress pending explicit owner-selected mappings and the bounded live presence matrix. |
| 2026-08-30 | P02-T05 | Owner rejected the redundant explicit notification target and authorized registering every person and associated device. A redacted read-only audit found four configured persons and five linked Mobile App tracker/notification associations: three one-device persons and one two-device person. A later uniqueness check proved that one notification endpoint is shared across persons, leaving four actual endpoints; no person entity itself owns a registry device, confirming the native relationship is indirect but complete. Corrected the living AGENTS/GOAL/plan contract and froze a schema-v4 revision-2 migration plus b2 deployment before implementation. | No Home Assistant state or repository code changed during discovery. The minimum design stores only persons, joins registry siblings without names, filters each tracker by its own `home` state, deduplicates the shared endpoint, calls only fixed `notify.send_message`, and uses supported subentry flows for registration. The major schema deliberately stays v4 so `v0.1.0b5` remains a loadable downgrade; P02-T04 remains open and b1 is superseded rather than configured. |
| 2026-08-30 | P02-T05 | Implemented person-only recipients, native Mobile App registry joins, per-tracker home filtering, multiple-device delivery, duplicate-target suppression, fail-closed missing/disabled/unavailable paths, schema-v4 revision-2 migration, translations, b2 metadata, rollback/status documentation, and focused production-boundary coverage. Ponytail removed the obsolete one-field recipient dataclass and a redundant migration branch. | Focused gate: 30 passed. Integration gate: 52 passed. Canonical gate: 166/166 at 95.71% with Ruff, format, strict mypy, artifact/secret, migration, replay, privacy, and zero-actuator checks green. Consolidated Ponytail review after corrections: `Lean already. Ship.` Publication, HACS installation, supported recipient flows, and redacted live verification remain. |
| 2026-08-30 | P02-T05 | Published commit `d80e9ad` on the feature and `release/0.2.0` branches, annotated/tagged and released `v0.2.0b2`, refreshed HACS, created a supported completed backup, downloaded the exact beta, and observed the required restart recovery on Home Assistant 2026.8.3. Added all four persons through the supported person-only subentry flow, then passed an explicit reload and final restart. | Final redacted gate: one loaded schema-v4 revision-2 entry; 4 rooms, 5 openings, 6 devices, 17 enabled unique entities; 4 recipients; five person-device associations resolving to four enabled/available unique endpoints; fixed notify surface; zero integration Repairs/logs/services/owned persistent notifications; no synthetic message or physical action. Eleven of twelve input classes are ready and one `missing_input` safely degrades one opening. The post-live canonical rerun passes 166/166 at 95.71%. P02-T05 terminates; P02-T04 retains only naturally observed delivery-matrix evidence. |
| 2026-08-30 | P02-T04/P02-T05 | Corrected a stale rollback assumption exposed by the final read-only audit. `v4.17_pre` is no longer present in the live entity registry or supported automation-config inventory after the owner accepted continuing on the custom component; it remains immutable only as a repository fixture. Updated current AGENTS/GOAL/README/runbook/status/plan references without rewriting Phase 00/01 historical evidence. | No Home Assistant mutation was made for this correction. `v0.1.0b5` remains the live-verified operational rollback. The change narrows current documentation to observed state and does not alter product behaviour, notification routing, privacy, or physical-action boundaries. |
| 2026-08-30 | P02-T06 | Started an owner-directed recipient correction after a redacted relationship audit proved that the owner's Mobile App endpoint is shared by the owner and `Codex` person, and both are currently configured recipients. Froze the minimum change as removal of only the `Codex` recipient subentry; the underlying Home Assistant person/tracker relationship and all other recipients remain out of scope. | No Home Assistant mutation yet. The ordinary path currently deduplicates the endpoint, but the `Codex` arrival path is semantically wrong; the supported subentry API, post-reload recipient graph, and zero-message/action gates must pass before termination. |
| 2026-08-30 | P02-T06 | Removed only the `Codex` recipient through Home Assistant's supported `config_entries/subentries/delete` WebSocket command. Corrected the living product/status/plan contract so shared trackers do not imply recipient authorization and the post-cutover rollback criterion names the integration fallback rather than the retired automation. No production code or underlying person/device association changed. | The entry reloaded and remains loaded with 3 recipients. The owner route and two other recipients remain; four configured links resolve to four distinct enabled/available targets with no configured shared target. Inventory remains 6 devices/17 entities, diagnostics report 3 recipients, and integration Repairs/logs/services/owned persistent notifications remain zero. No synthetic notification or physical action was sent. Focused artifact/privacy verification and the canonical 166/166 gate pass at 95.71%. |
| 2026-08-30 | P02-T07 | Started an owner-directed presentation correction from the first naturally observed b2 notification. Froze the minimum change at the shared formatter plus reason propagation already available from policy: two multiline sections, context-sensitive non-duplicated labels, changed/actionable components only, and concise safety context. | No production file or Home Assistant state changed at task start. The established `notify.send_message` action, presence/recipient resolution, no-queue rule, conservative rain/wind/geometry policy, and zero-actuator boundary remain frozen. Home Assistant best practices keep the native notification surface; Ponytail rejects a template engine or new presentation abstraction. |
| 2026-08-30 | P02-T07/P02-T04 | Implemented the shared ordinary/arrival formatter, propagated the existing policy reason through transient candidates, reproduced the owner-proposed Spanish sections, and prepared local candidate `v0.2.0b3` with updated release/runbook/status contracts. Single-opening rooms now omit orientation/title duplication; multi-opening rooms retain a non-duplicated suffix; unchanged and degraded components are absent. | Focused notification gate: 28 passed; focused notification plus manifest gate: 31 passed. Canonical gate: 167 passed at 95.75% with Ruff, format, strict mypy, diff, artifact, secret, replay, privacy, and zero-actuator checks green. The first focused run exposed an incorrect new test expectation for a wind-policy candidate; correction verified that the real `WIND_CLOSE` reason is preserved. Consolidated Ponytail review: `Lean already. Ship.` No safety threshold, source, recipient, entity, queue, dependency, or service changed. Publication, HACS installation, restart, and redacted live b3 verification remain. |
| 2026-08-30 | P02-T07/P02-T04 | Published commit `f42817d` on the feature and beta release branches, created annotated tag and public prerelease `v0.2.0b3`, refreshed the exact custom HACS repository, confirmed the beta, and used its native update entity after a recent supported backup. One required restart completed and the component returned loaded; no configuration, recipient, helper, message, or physical action was created or changed. | Post-restart evidence confirms Home Assistant 2026.8.3, installed b3/schema v4, one loaded entry, 4 rooms, 5 openings, 6 devices, 17 unique enabled entities, 3 recipients, available native notification action, and zero duplicate identities, integration Repairs/logs/services/owned persistent notifications. Two currently missing room-temperature inputs explicitly degrade three openings, leaving 10/12 source classes, 2/5 recommendations and 11/17 entities ready; all reported blind targets are bounded and every non-closed target remains positive. P02-T07 terminates; P02-T04 retains natural-device presentation and presence-matrix observation. |
| 2026-08-30 | P02-T08/P02-T09 | Owner confirmed wind-driven rain semantics and questioned the notification source. Read-only Home Assistant inspection found both installed b3 and one enabled notification-capable legacy advisor automation; the automation ran in the prior 24 hours and all four retained traces reached its notify action. The owner's screenshot wording matches the b2 component formatter, proving that the component also delivered while the legacy route remained able to send separate advice. | Froze a reversible single-route cutover before any mutation and a separate façade/gust policy correction. Existing tests and production tracing show that b3 closes every rainy unprotected opening; the accepted replacement keeps missing/stale fail-safe and the 45 km/h all-façade close while allowing no-gust/leeward optimizer targets. No Home Assistant or production state changed during this discovery step. |
| 2026-08-30 | P02-T08 | Completed the safe-refactoring audit across live automations, scripts, scenes, state attributes, config-entry data/options and the readable dashboard. No executable caller references the legacy advisor; its three dashboard occurrences are display-only and the same dashboard already contains all 17 component entities. Turned off only the exact legacy automation with `stop_actions` through the supported service and kept its entity/configuration intact for reversible rollback. | Post-cutover evidence: legacy automation `off` with no new trace; installed b3 remains one loaded schema-v4 entry with 4 rooms, 5 openings, 3 recipients and available `notify.send_message`. No synthetic notification, config-entry/recipient/dashboard change, restart, physical service or action occurred. Focused notification/lifecycle gate: 13 passed. Canonical gate: 167 passed at 95.75% with lint, formatting, strict typing, replay, privacy, artifact/secret and zero-actuator checks green. P02-T08 terminates and P02-T09 starts under the root-owned safety contract. |
| 2026-08-30 | P02-T09 | Implemented the owner-approved minimum at the shared pure policy boundary: rain restriction now requires at least 0.5 km/h of raw gust projected onto the façade; otherwise the optimizer target proceeds through the existing wind limits. Kept exposed protected-tilt geometry, the 45 km/h all-façade close, missing/stale degradation, notification reasons and blind targets unchanged. The live capability audit found no distinct gust-direction entity or weather attribute, so the existing typed direction input is reused without a selector, schema or migration. Updated ADR/product/release/runbook/status contracts for b4. | Exact focused gate: 75 passed; focused gate plus manifest: 78 passed. Canonical gate: 179 passed at 95.75% with Ruff, formatting, strict mypy, artifact/secret, cardinal/intercardinal and near-zero boundaries, production adapter, replay, notification, privacy and zero-actuator checks green. Nominal replay remains valid and every non-closed stable target retains a positive blind opening. Consolidated Ponytail review: `Lean already. Ship.` Root retained the task because it changes the public safety boundary; publication and live deployment remain. |
| 2026-08-30 | P02-T09 | Published commit `ee69f38` on the feature and beta release branches, created annotated tag and public prerelease `v0.2.0b4`, refreshed the exact HACS repository and confirmed prerelease tracking. The automatic-backup action produced no verifiable artifact, so it was rejected; a supported full Supervisor backup including the database then completed before the native update entity downloaded exactly b4. Configuration validation passed and one observed down/up restart recovered on Home Assistant 2026.8.3. | Post-live gate: installed b4/schema v4; one loaded entry, 4 rooms, 5 openings, 3 recipients, 6 devices and 17 enabled unique entities; native notify action available; legacy automation present and off; zero duplicate identities, integration Repairs/logs/services/owned persistent notifications. Current external input degradation remains explicit at 10/12 ready source classes, 2/5 ready recommendations and 11/17 available entities; all emitted blind targets are bounded and each non-closed target is positive. No synthetic message, configuration/recipient change or physical action occurred. P02-T09 terminates; P02-T04 retains only natural-device presentation and presence/arrival evidence. |
| 2026-08-30 | P02-T10 | Started the owner-directed room-source replacement. Read-only registry and supported reconfigure-flow inspection found four configured rooms and six SONOFF display temperature/humidity devices. Calibration-number siblings were excluded by requiring native `sensor` domain and measurement classes. | No Home Assistant configuration changed during discovery. The minimum change reuses the existing room subentry flow, replaces only temperature/humidity fields, preserves room names and optional CO2, renames no entity, adds no helper/schema/code, reloads once after all four updates, and rolls back through the same flows if any submission or post-gate fails. |
| 2026-08-30 | P02-T10 | Completed the pre-mutation identity audit. Three rooms have one unique room-labelled display sensor. The remaining configured room has no textual match among the other three display devices; all three lack an area assignment, its old source is unavailable, and the Recorder history contains no valid sample for correlation. Creation time is not identity evidence and the best global text assignment is tied, so the earlier tentative newest-cohort assumption was removed from the living task. | P02-T10 is blocked before mutation pending one explicit owner mapping or a matching registry rename. All six devices and the three unambiguous candidates remain untouched; no config flow was submitted, no entry reloaded, and no notification or physical action was called. |
| 2026-08-30 | P02-T10 | Corrected the room model after the owner identified all six display devices: Salón, Baño, Despacho, Cocina and the two bedrooms Anto/Eli, which share orientation. A read-only comparison of the live subentries, immutable v4.17 fixture and legacy source proved that the component was initially deployed with Salón, Despacho, Cocina and one generic Dormitorio; the old Baño block was only a comment marked pending its temperature source. | No room was deleted during P02-T10 and no Home Assistant mutation occurred. Each of the six display devices has exactly one enabled native temperature sensor and one enabled native humidity sensor. P02-T10 now blocks only on the physical bedroom-opening topology: one generic bedroom opening exists, while common orientation alone does not establish ownership or the other geometry of a second opening. The all-or-nothing source update remains untouched. |
| 2026-08-30 | P02-T10 | Reopened and corrected the historical topology audit after the owner pointed out that Baño already had its window and overhang configured. The full predecessor source and handoff prove seven intended openings: the executable five plus Dormitorio 2 NE and Baño SO, both commented only because their real temperature entities were pending. Dormitorio 1/2 have identical retained geometry; Baño retains its own façade, height, overhang gap/depth and rain protection. | The earlier wording conflated non-executable pending sensor linkage with absent physical configuration and is superseded. A read-only live comparison shows the five migrated openings use 1.60 m width, 1.20 m height, tilt support and manual blinds consistently, supplying the integration-only fields absent from the width-agnostic predecessor. P02-T10 resumes with a six-room/seven-opening transactional target; Home Assistant remains untouched at this checkpoint. |
| 2026-08-30 | P02-T10 | Restored the complete topology through supported subentry flows: reassigned the four existing rooms to their exact display temperature/humidity pairs, reused the generic bedroom identity/opening for Anto, and created Eli plus Baño rooms/openings from the recovered geometry. The first all-in-one attempt submitted optional opening selectors as null; Home Assistant rejected that step and removed the newly created rooms, but the same nulls prevented the room rollback, leaving the four desired source updates applied. An immediate supported read-back exposed the partial state; the corrected forward pass omitted absent optional fields and completed the target. | Final live gate: loaded schema v4; 6 rooms, 7 openings, 3 recipients, 8 integration devices and 23/23 available unique entities; 14/14 source classes ready; seven recommendation, blind and safety triples with zero invalid target; all 17 prior dashboard references still resolve; legacy automation off; zero duplicate identities, Repairs, integration errors/services or owned persistent notifications. No entity-registry rename, synthetic message or physical action was called. Focused integration gate: 32 passed. Canonical gate: 179 passed at 95.75% after deleting only verified regenerable repository caches and disabling Ruff/mypy cache writes because C: had zero free bytes. P02-T10 terminates; the extra diagnostic/recovery reloads are the recorded deviation from the planned single final reload. |
